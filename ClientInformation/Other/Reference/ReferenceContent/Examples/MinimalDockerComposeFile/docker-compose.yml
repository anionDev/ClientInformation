version: '3.2'

services:
  clientinformation:
    image: clientinformation:0.1.3
    container_name: 'clientinformation'
    ports:
      - "443:443"
    volumes:
      - "./Volumes/Configuration:/Workspace/Configuration"
