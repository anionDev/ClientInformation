# ClientInformationLibrary-reference

## Overview

`ClientInformationLibrary` is a shared .NET library containing common types and helpers used by the other codeunits of this repository (`ClientInformation` and `ClientInformationBackend`).

## Hints

See also the [hints](./Hints.md) (mainly for developer).
